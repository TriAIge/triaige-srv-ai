aws:
  s3:
    endpoint: ""         # Usar a AWS real
    force-path-style: false

gemini:
  timeout-seconds: 90
  max-retries: 3
  retry-backoff-ms: 3000

logging:
  level:
    br.com.triaige: INFO
    org.springframework.web: WARN
